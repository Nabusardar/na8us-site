# 📚 NABUS Server - AEZA Documentation

## 🖥️ Информация о сервере

- **Hostname:** excitedtomato.aeza.network
- **IP:** 45.80.228.254
- **SSH доступ:** `ssh root@45.80.228.254`
- **ОС:** Ubuntu Linux (kernel 6.8.0-90-generic)
- **Пользователь:** root

---

## 📁 Структура проекта на сервере

Путь к проекту: `/root/my-server/`

```
my-server/
├── site/              # Файлы сайта (NABUS EXPANSION)
│   ├── index.html     # Главная страница
│   ├── script.js      # JavaScript код
│   ├── style.css      # Стили
│   └── .git/          # Git репозиторий
├── Caddyfile          # Конфигурация веб-сервера
├── docker-compose.yml # Docker конфигурация
├── n8n_data/          # Данные n8n
├── caddy_config/      # Конфиг Caddy
├── caddy_data/        # Данные Caddy
├── db/                # База данных
└── cert/              # SSL сертификаты
```

---

## 🚀 Работающие сервисы

### 1. Caddy (Веб-сервер)
- **Порты:** 80, 443
- **Домены:** 
  - na8us.com (основной сайт)
  - n8n.na8us.com (n8n автоматизация)
- **Статус:** ✅ Работает

### 2. n8n (Автоматизация)
- **Порт:** 5678 (только localhost)
- **URL:** https://n8n.na8us.com
- **Статус:** ✅ Работает

### 3. SSH
- **Порт:** 22
- **Доступ:** `ssh root@45.80.228.254`

---

## 🔧 Основные команды

### Подключение к серверу:
```bash
ssh root@45.80.228.254
```

### Перезапустить сервисы:
```bash
cd /root/my-server
docker-compose restart
```

### Посмотреть логи Caddy:
```bash
docker logs caddy
```

### Посмотреть логи n8n:
```bash
docker logs n8n
```

### Редактировать файлы сайта:
```bash
cd /root/my-server/site
nano index.html  # или script.js, style.css
```

### Проверить статус Git:
```bash
cd /root/my-server/site
git status
git log --oneline -5
```

---

## 🌐 Домены и URLs

- **Сайт:** https://na8us.com
- **n8n:** https://n8n.na8us.com
- **IP сервера:** 45.80.228.254

---

## ⚠️ Важно

### Состояние Git репозитория:
- Ветка `main` отстает от origin/main на 3 коммита
- Есть незакоммиченные изменения
- Удалены папки: assets/, backup/, logs/, resume.html
- Изменены: index.html, script.js, style.css

### Следы VPN:
- ❌ НЕ ОБНАРУЖЕНО
- Сервер чистый, никаких VPN процессов нет

---

## 📝 Редактирование сайта

### Вариант 1: Прямо на сервере
```bash
ssh root@45.80.228.254
cd /root/my-server/site
nano index.html
```

### Вариант 2: С локального Mac
1. Скопировать файлы с сервера
2. Редактировать на Mac
3. Загрузить обратно:
```bash
scp index.html root@45.80.228.254:/root/my-server/site/
```

### Вариант 3: Git (рекомендуется)
```bash
# На сервере
cd /root/my-server/site
git add .
git commit -m "Описание изменений"
git push
```

---

## 🔄 Синхронизация файлов

### Скачать файлы с сервера на Mac:
```bash
mkdir -p ~/nabus-site-backup
cd ~/nabus-site-backup
ssh root@45.80.228.254 "cat /root/my-server/site/index.html" > index.html
ssh root@45.80.228.254 "cat /root/my-server/site/script.js" > script.js
ssh root@45.80.228.254 "cat /root/my-server/site/style.css" > style.css
```

### Загрузить файлы на сервер:
```bash
cd ~/nabus-site-backup
scp index.html root@45.80.228.254:/root/my-server/site/
scp script.js root@45.80.228.254:/root/my-server/site/
scp style.css root@45.80.228.254:/root/my-server/site/
```

---

## 📅 Последние обновления

- **Дата создания:** 2026-02-05
- **Последняя проверка:** 2026-02-05
- **Статус сервера:** ✅ Все работает
- **Git статус:** Есть незакоммиченные изменения

---

## 🆘 Быстрая справка

| Задача | Команда |
|--------|---------|
| Подключиться | `ssh root@45.80.228.254` |
| Перезапустить сайт | `cd /root/my-server && docker-compose restart caddy` |
| Перезапустить n8n | `cd /root/my-server && docker-compose restart n8n` |
| Посмотреть логи | `docker logs caddy -f` |
| Редактировать файл | `nano /root/my-server/site/index.html` |
| Git статус | `cd /root/my-server/site && git status` |

---

🎯 **NABUS EXPANSION** - Архитектура цифрового превосходства
